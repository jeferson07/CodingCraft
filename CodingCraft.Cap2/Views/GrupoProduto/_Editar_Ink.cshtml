@model CodingCraft.Cap2.Models.Entities.GrupoProduto

@{
    ViewBag.Title = "_Editar_Ink";
}

	<h2>Editar</h2>

		@using (Html.BeginForm(null, null, FormMethod.Post, new { @class = "ink-form"})) {
		@Html.ValidationSummary(true)
		<fieldset>
			<legend>GrupoProduto</legend>

				@Html.HiddenFor(model => model.GrupoProdutoId)
				@Html.Partial("_CriarOuEditar_Ink", Model)

			<p>
				<input type="submit" value="Salvar" class="ink-button"/>
			</p>
		</fieldset>
	}

	<div>
		@Html.ActionLink("Voltar para Lista", "Index")
	</div>

