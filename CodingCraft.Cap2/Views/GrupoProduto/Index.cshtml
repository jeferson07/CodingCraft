@model IEnumerable<CodingCraft.Cap2.Models.Entities.GrupoProduto>


@if (Session["Layout"] != null && Session["Layout"].ToString() == "Ink")
{
	@Html.Partial("_Index_Ink", Model)
}
else if (Session["Layout"] != null && Session["Layout"].ToString() == "Foundation")
{
    @Html.Partial("_Index_Foundation", Model)
}
else
{
    @Html.Partial("_Index_Bootstrap", Model)
}

